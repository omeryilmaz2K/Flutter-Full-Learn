import 'package:flutter/material.dart';

class Demo101AppPaddings {
  static const pagePadding = EdgeInsets.all(20);
  static const imagePadding = EdgeInsets.symmetric(vertical: 30);
  static const subtitlePadding = EdgeInsets.symmetric(vertical: 15);
  static const buttonPadding = EdgeInsets.all(20);
}